spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=Cadastro2024;encrypt=false
    username: cadastro
    password: Teste_123#
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.SQLServerDialect
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080